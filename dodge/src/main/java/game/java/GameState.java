package game.java;

/** Enums for game state*/
public enum GameState {
    WELCOME,
    ACTIVE_GAME,
    GAME_OVER,
    PAUSED,
    SELECT_DIFFICULTY,
    STORE,
    INFO,
    VICTORY,
}
